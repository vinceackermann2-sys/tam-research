name: AERA hardware-aware CPU learned gate v3

on:
  issues:
    types: [opened]

permissions:
  contents: read
  issues: write

jobs:
  validate:
    if: >-
      github.event.issue.user.login == github.repository_owner &&
      startsWith(github.event.issue.title, '[aera-hardware-cpu-v3]')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install CPU PyTorch
        run: pip install 'torch==2.10.0' --index-url https://download.pytorch.org/whl/cpu
      - name: Install repository
        run: pip install -e .
      - name: Run unchanged learned gate with predictive recurrent stream
        shell: bash
        run: |
          set -o pipefail
          python aera_hardware_cpu_gate_v3.py 2>&1 | tee /tmp/aera-hardware-cpu-v3.log
          status=${PIPESTATUS[0]}
          if [ "$status" -ne 0 ]; then exit "$status"; fi
          tail -n 1 /tmp/aera-hardware-cpu-v3.log > /tmp/aera-hardware-cpu-v3.json
      - name: Post pass
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          {
            echo '🧪 **Hardware-aware AERA v3 predictive-stream CPU gate complete.**'
            echo
            echo '```json'
            cat /tmp/aera-hardware-cpu-v3.json
            echo '```'
            echo
            echo 'The original >=90% carried-boundary threshold and all other v1 gates were retained. This pass still requires integrated L4 and real-language validation before 100M.'
          } > /tmp/comment.md
          gh issue comment "$ISSUE_NUMBER" --body-file /tmp/comment.md
      - name: Post failure
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          {
            echo '❌ **Hardware-aware AERA v3 predictive-stream CPU gate failed.** 100M remains blocked.'
            echo
            echo '```text'
            if [ -f /tmp/aera-hardware-cpu-v3.log ]; then tail -n 120 /tmp/aera-hardware-cpu-v3.log; else echo 'no log'; fi
            echo '```'
          } > /tmp/comment.md
          gh issue comment "$ISSUE_NUMBER" --body-file /tmp/comment.md
