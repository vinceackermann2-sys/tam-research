name: Phase-31 registered rep1 SEALED — no execution

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sealed-noop:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse any further Phase-31 rep1 execution
        run: |
          echo 'Phase-31 registered replicate 1 is sealed as a decisive FAIL.'
          echo 'Seed base 831310000 is consumed and must never be retried.'
          echo 'Replicate 2 (831310100) and replicate 3 (831310200) remain locked unconsumed.'
          exit 1
